use_default_settings: true

server:
  limiter: false
  # Generate a fresh secret before first use:
  #   openssl rand -hex 32
  secret_key: "CHANGE_ME"

search:
  formats: [html, json, rss, csv]
  # patched-by-lucy-internet-v0
