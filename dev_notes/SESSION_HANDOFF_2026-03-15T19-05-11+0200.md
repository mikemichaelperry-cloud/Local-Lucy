# Session Handoff

Date: 2026-03-15 19:05:11 +0200
Active root: `/home/mike/lucy/snapshots/opt-experimental-v5-dev`

## Completed This Session

Implemented a bounded medication-detector broadening pass without changing authority boundaries.

Main outcome:

- the shared medication seam now generalizes better across common medication prompts without depending only on one-off hardcoded drug additions

## What Changed

Shared detector changes:

- `tools/router/core/medical_query_heuristics.py`
  - now combines alias normalization, medication-question pattern detection, bounded candidate extraction, and conservative medication-token heuristics
  - emits a compact `medical_detector` object with provenance and confidence
- `tools/router/medical_query_heuristics.py`
  - now supports JSON detector output for debugging/trace use

Router / mapper / trace changes:

- `tools/router/core/intent_classifier.py`
  - now attaches `medical_detector` to classifier output
- `tools/router/plan_to_pipeline.py`
  - now threads a truthful `medical_detector` object through mapper output
  - preserves both literal original prompt and resolved execution query
- `tools/router/execute_plan.sh`
  - now exposes `medical_detector` in dryrun, execution trace, and outcome fields

Shared normalization config:

- `config/evidence_normalization_aliases_v1.tsv`
  - extended with a small set of common brand/generic aliases:
    - panadol/paracetamol
    - tylenol/acetaminophen
    - advil/motrin/nurofen/ibuprofen
    - lipitor/atorvastatin
    - cialis/tadalafil
    - viagra/sildenafil
    - levitra/vardenafil
    - glucophage/metformin

Tests:

- updated `tools/tests/test_query_policy_shared_heuristics.sh`
- added `tools/tests/test_medication_detector_broader_coverage.sh`
- added `tools/tests/test_execute_plan_medication_detector_trace.sh`

## Behavior Now

The governor more reliably contracts evidence-required medical handling for prompts such as:

- `What are the side effects of ibuprofen?`
- `Does tadalafil interact with alcohol?`
- `What is Tadalifil?`
- `What does amoxycillin do?`
- `Is Lipitor safe with grapefruit?`
- `Dose of Panadol?`

The detector remains bounded:

- no LLM answer authority was added
- no interpreter assist was required
- evidence remains the authority for answers

## Validation

Targeted tests passed:

- `bash tools/tests/test_query_policy_shared_heuristics.sh`
- `bash tools/tests/test_medication_detector_broader_coverage.sh`
- `bash tools/tests/test_execute_plan_medication_detector_trace.sh`
- `bash tools/tests/test_execute_plan_ibuprofen_routes_medical_evidence.sh`
- `bash tools/tests/test_medical_allowlist_plumbing.sh`
- `bash tools/tests/test_semantic_trace_preserves_literal_original_query.sh`
- `bash tools/tests/test_execute_plan_semantic_interpreter_guardrails.sh`
- `bash tools/tests/test_execute_plan_semantic_interpreter_trace_and_fallback.sh`

Behavior checks:

- dry route for `What are the side effects of ibuprofen?`
  - `PIPELINE=EVIDENCE`
  - `OUTPUT_MODE=VALIDATED`
- live route for the same prompt
  - `BEGIN_VALIDATED`
  - `Insufficient evidence from trusted sources.`
  - `END_VALIDATED`

Fresh stabilization battery passed:

- `TOTAL_CHECKS=30`
- `FAILED_CHECKS=0`
- `WARNED_CHECKS=0`
- `OVERALL=PASS`
- report: `/home/mike/lucy/snapshots/opt-experimental-v5-dev/tmp/test_reports/stabilization_battery/20260315T190330+0200/report.txt`

## Current Risk / Next Likely Work

- The detector is broader but still intentionally bounded; uncommon brands and harder misspellings may need future additions if they fall outside the current pattern-plus-candidate logic.
- No fresh latency benchmark was run in this session.
- If future work is needed, the next safe extension is likely small catalog growth or tighter candidate heuristics, not an ontology or answer-authoritative LLM layer.

## Artifacts

Desktop report:

- `/home/mike/Desktop/LOCAL_LUCY_MEDICATION_DETECTOR_BROADENING_REPORT_2026-03-15T19-05-11+0200.md`

Stabilization report:

- `/home/mike/lucy/snapshots/opt-experimental-v5-dev/tmp/test_reports/stabilization_battery/20260315T190330+0200/report.txt`
