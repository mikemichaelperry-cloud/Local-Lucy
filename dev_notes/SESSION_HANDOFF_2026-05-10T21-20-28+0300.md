# Local Lucy v8 — Session Handoff

**Date:** 2026-05-10  
**GitHub main:** `05d04fc5` (8 commits ahead of last handoff)  
**Test status:** 79/79 pass, 0 xfail

---

## What Was Built Tonight

### 1. Memory-Aware Routing Gate (`c290c832`)
- `_memory_routing_gate()` in `classify.py` overrides live-data routes (WEATHER, NEWS, TIME, STOCKS, AUGMENTED) → LOCAL when:
  - Session memory is enabled (`LUCY_SESSION_MEMORY=1`)
  - Query has pronoun/follow-up/recall markers
  - Recent conversation turns exist in SQLite
  - Query does NOT contain live-data keywords
- 16 unit tests, kill switch (`LUCY_MEMORY_GATE=0`), 2-5ms overhead

### 2. Learning Candidate Generator (`7bd8363f`)
- `generate_learning_candidates.py` — mines router logs, feedback buffer, user corrections, auto-feedback
- Read-only by default; outputs reviewable JSONL
- `--apply` delegates to `background_learner.py`
- Currently dormant (no router logs yet — see #1 below)

### 3. 70 New Training Examples (`ff2b9ccc`)
- Filled 7 gaps: DIY, ambiguous boundaries, pronouns, guard bypasses, typos, compound intent, cultural
- Index: 395 → 463 examples
- 0 duplicates verified by similarity audit

### 4. Keyword Guard Tightening (`6150376d` + `2010cead`)
- Removed: `dry`, `wet`, `rain`, `kid`, `stock` from overbroad keyword lists
- Added: non-Earth weather exclusion (Mars → LOCAL), historical price exclusion ("in 2010" → LOCAL)
- Added: exact-match boost (sim ≥ 0.999 → trust self-route over k-NN vote)
- Added: news guard respects confident LOCAL embedding ("Tell me about the news industry")
- Router accuracy on adversarial suite: 59% → **100%** (22/22)

### 5. Feedback Trigger Bug Fix (`a9c43158`)
- `trigger_background_learning()` now correctly triggers on `user_feedback.jsonl` entries
- Previously only checked `auto_feedback.jsonl` (always empty) — user corrections were ignored

### 6. Router Decision Logging (`847d4435`)
- `START_LUCY.sh` now sets `LUCY_ROUTER_LOG_DIR`
- Enables `generate_learning_candidates.py` and `background_learner.py` to function

### 7. TIME Timezone Fix (`847d4435` + `05d04fc5`)
- Bare "What time is it?" no longer extracts "What" as location
- Defaults to system local timezone (was failing with "Could not determine timezone")
- Blacklist simplified from 120 words to 38

### 8. Adversarial Routing Test Suite (`847d4435` + `2010cead`)
- `test_routing_edge_cases.py` — 22 cases, 82% threshold
- Categories: DIY, ambiguous, pronoun, guard bypass, typos, compound
- **All 22 pass, 0 xfails**

---

## Active Todo List

### 🔴 High Priority

1. **Monitor router logs for learning signal**
   - `LUCY_ROUTER_LOG_DIR` is now set in `START_LUCY.sh`
   - After ~1 week of usage, run `generate_learning_candidates.py` and review output
   - Goal: find real misroutes the embedding router misses

2. **Refactor `execution_engine.py`**
   - Currently 4,309 lines — too large for a single file
   - Extract evidence fetchers (weather, news, time, augmented) to `tools/router_py/evidence/`
   - Extract formatters to `tools/router_py/formatters/`
   - Extract memory loader to `tools/router_py/memory_loader.py`
   - Do this **incrementally** — extract a piece when you next touch it, not a big bang refactor
   - Target: reduce to ~2000 lines over 2-3 sessions

### 🟡 Medium Priority

3. **Fix known routing gap: "How do I change a tire?"**
   - Still routes AUGMENTED (was in index before 70 examples, still misroutes)
   - The new examples didn't fix it because the embedding vote is still borderline
   - Options: add more DIY examples (10-20), tighten DIY keyword detection, or lower confidence threshold

4. **Add raw-turn keyword search to memory**
   - Currently only summary-level semantic search
   - If a detail was in a turn that got summarized away, retrieval depends on summary quality
   - SQLite `fts5` full-text search over `conversation_turns.text` as fallback

5. **Bump memory context cap or make it dynamic**
   - Currently 500 chars — very tight
   - Consider 800-1000 for text surface, shorter for voice

### 🟢 Backburner

6. **Conditional LLM resolver**
   - Fully designed, not implemented
   - Trigger: `avg_sim < 0.40`, guard-embedding disagreement, near-tie k-NN
   - Model: Llama 3.2 1B Instruct Q4_K_M, CPU-only, lazy load
   - Activate only after router logs show ≥2% misroute rate on real queries
   - Architecture documented in `.kimi/plans/rocket-wasp-luke-cage.md`

---

## Key Files Changed

| File | Status | Note |
|------|--------|------|
| `tools/router_py/classify.py` | Modified | Memory routing gate + logging field |
| `tools/router_py/execution_engine.py` | Modified | TIME timezone fix |
| `models/router/hybrid_router.py` | Modified | Guard tightening + exact-match boost |
| `models/router/comprehensive_index.jsonl` | Modified | +68 examples |
| `models/router/comprehensive_examples.json` | Modified | +68 examples |
| `models/router/comprehensive_embeddings.npy` | Modified | Rebuilt (463, 768) |
| `models/router/append_augmented_examples.py` | New | Script to append curated examples |
| `models/router/generate_learning_candidates.py` | New | Mines usage data for training |
| `models/router/background_learner.py` | Modified | Fixed user feedback trigger |
| `models/router/test_feedback_trigger.py` | New | Reproduction test for trigger bug |
| `tools/router_py/test_memory_gate.py` | New | 16 tests for memory routing gate |
| `tools/router_py/test_routing_edge_cases.py` | New | 22 adversarial routing tests |
| `START_LUCY.sh` | Modified | Router logging enabled |

---

## Test Summary

```
79 passed in 18.31s
- test_main.py: 17 pass
- test_python_execution_path.py: 12 pass
- test_memory_integration.py: 6 pass
- test_runtime_toggles.py: 2 pass
- test_routing_modes.py: pass
- test_memory_gate.py: 16 pass
- test_routing_edge_cases.py: 22 pass (100%)
- test_feedback_trigger.py: 2 pass (reproduction + verification)
```

---

## Hardware Context

- **CPU:** AMD Ryzen 5600X
- **RAM:** 32 GB
- **GPU:** RTX 3060 12GB
- **Model:** Qwen3 14B Q4_K_M (local-lucy)
- **Router:** ModernBERT-base on CPU, 463 examples
- **OS:** Ubuntu 22.04
