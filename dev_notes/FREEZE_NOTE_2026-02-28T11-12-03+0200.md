# FREEZE NOTE

This snapshot is a known-good freeze.

Source snapshot:
- /home/mike/lucy/snapshots/opt-experimental-v2-dev

Freeze purpose:
- Preserve routing + allowlist + medical plumbing stabilization state.
- Provide a rollback point and analysis reference.

Verification:
- ./tools/tests/run_stabilization_battery.sh
- Expect OVERALL=PASS and FAILED_CHECKS=0

Key stabilized areas:
- allow_domains_file passthrough (router -> executor -> gate)
- medical allowlist filter plumbing
- tier/category drift audit
- debug visibility gated by LUCY_DEBUG_ROUTE=1

This freeze is immutable. Do not patch it. Clone for further development.
